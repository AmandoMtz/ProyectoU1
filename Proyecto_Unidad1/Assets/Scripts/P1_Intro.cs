using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class P1_Intro : MonoBehaviour //todas las clases son de MonoBehaviour dos metodos 1 start 1 update
{
    // Start is called before the first frame update
    void Start() //una sola vez y el update por 1 frame
    {

     

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
